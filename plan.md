# 사주 보기(saju.html) 프로젝트 — 진행 리뷰 및 계획

## 1. 목표
Gemini API를 이용해 생년월일 정보를 입력받아 사주를 간단히 봐주는 로컬 웹앱을 만든다.
처음부터 사주 계산 로직을 다 짜지 않고, 우선 "입력 → API 호출 → 결과 표시"가 되는 기본 구조부터 검증한다.

## 2. 아키텍처 결정
| 구성 | 선택 | 이유 |
|---|---|---|
| API 키 위치 | `.env` (서버에서만 로드) | `saju.html`이 브라우저에서 직접 Gemini를 호출하면 API 키가 그대로 노출됨. 로컬 전용이라도 키 유출 방지를 위해 Flask 백엔드가 키를 들고 있고, 프론트는 백엔드에만 요청. |
| 백엔드 | Flask (`app.py`) | 가장 가벼운 구조로 `.env` 로드 + REST 프록시 역할만 하면 되므로 충분. |
| 프론트 | `saju.html` 단일 파일 | 아직 로직이 단순해서 별도 프레임워크 불필요. |
| Gemini 연동 방식 | REST API 직접 호출 (`requests`) | [공식 문서](https://ai.google.dev/gemini-api/docs/get-started?hl=ko#rest) 기준 `interactions` 엔드포인트 사용. |

## 3. 진행 과정 타임라인

1. **Gemini API 테스트** — `test_gemini.py`로 Python SDK 방식 우선 확인.
2. **환경 설정** — `.gitignore`, `.env` 생성. `.venv` + `uv`로 패키지 관리(시스템 Python이 `externally-managed`라 전역 설치 불가 → 프로젝트 venv로 전환).
3. **saju.html 기본 구조 설계** — 아키텍처를 두 가지(백엔드+프론트 vs 순수 정적 파일) 중 사용자가 "백엔드+프론트" 방식으로 결정.
4. **Flask 서버 + 폼 작성** — 이름/생년월일/시간/성별 입력 → `/api/saju` 호출 → 결과 출력.
5. **버그 수정 #1 — API 응답 파싱 오류**
   - 증상: `KeyError: 'content'`
   - 원인: Gemini 응답의 `steps` 배열에 `thought`(추론) 스텝이 먼저 오고 실제 답변인 `model_output` 스텝은 그다음에 옴. `steps[0]`으로 고정 접근한 게 문제.
   - 해결: `type == "model_output"`인 스텝을 찾아서 텍스트 추출하도록 수정.
6. **UX 개선 — 생년월일 입력**
   - 문제: `<input type="date">` 캘린더 UI는 나이 많은 사용자가 수십 년 전 연도를 선택하기 어려움.
   - 해결: 년/월/일을 각각 `<select>` 드롭다운으로 분리 (연도는 올해부터 100년 전까지).
7. **버그 수정 #2 — "확인 중..."에서 멈춤**
   - 증상: `saju.html`을 열고 제출하면 10분이 지나도 응답 없음.
   - 원인: 파일을 더블클릭(`file://`)으로 직접 열어서 실행 — 이 경우 `fetch("/api/saju")`가 백엔드(`http://127.0.0.1:5000`)에 도달할 수 없음. 게다가 fetch 실패 시 에러 처리가 없어서 화면이 그대로 멈춤.
   - 해결: `try/catch` 추가로 연결 실패 시 원인과 해결법을 화면에 표시. (근본 해결책은 `python app.py` 실행 후 `http://127.0.0.1:5000`으로 접속하는 것.)
8. **커밋 & 푸시** — `.env` 제외하고 `.gitignore`, `app.py`, `saju.html`, `test_gemini.py` 커밋 (`e8a654d`).

## 4. 현재 상태 (완료)
- 입력 폼 → Flask 백엔드 → Gemini REST API → 결과 표시까지 엔드투엔드 동작 확인됨.
- API 키는 `.env`에만 있고 git에는 올라가지 않음.
- 로컬(`http://127.0.0.1:5000`)에서 실행 가능.

## 5. 아직 비어있는 부분 (TODO)
- **사주 계산 로직 자체가 없음.** 현재 `app.py`의 `build_prompt()`는 생년월일/시간/성별을 그냥 텍스트로 나열해서 Gemini에게 통째로 물어보는 수준. 실제 사주명리학의 만세력(연/월/일/시 천간지지 변환, 절기 기준 년도 계산 등)은 전혀 계산하지 않고 Gemini의 자체 지식에 의존 중.
- 입력 검증 없음 (예: 2월 30일 같은 잘못된 날짜도 그냥 통과됨).
- 에러 응답(백엔드 500 등)에 대한 사용자 친화적 메시지 없음.
- 결과 화면 디자인/스타일 없음 (순수 텍스트).
- 배포/실행 자동화 없음 (매번 `.venv/Scripts/python.exe app.py` 수동 실행).

## 6. 다음 단계 제안
1. 사주 계산 로직을 직접 구현할지, 계속 Gemini에게 맡길지 방향 결정.
   - 직접 구현 시: 만세력 라이브러리(예: `sxtwl`, `lunardate` 등) 검토 필요.
   - Gemini에 맡길 시: 프롬프트에 절기 기준 계산을 명시적으로 지시해서 정확도 개선.
2. 날짜 유효성 검증 (월별 일수 제한) 추가.
3. 결과 화면 최소한의 스타일링.
4. 필요하면 실행 스크립트(배치파일 등) 추가해서 매번 명령어 안 치게 하기.
